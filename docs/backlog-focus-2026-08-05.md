# Backlog Focus — 2026-08-05

Working list of everything currently in the backlog. This is the sole focus until items are cleared — do not pick up new work outside this list without updating it first. Kept local, not committed (same convention as `docs/audits/*.md`).

Per the [[feedback_doc_surface_sync]] rule: whenever an item here changes status, update this file AND the memory file it links to AND ROADMAP.md (if the item has a row there) in the same pass.

## Bugs — FIXED 2026-08-05, published `ticketlens@0.38.16` (beta)

1. ~~**`ticket_create` MCP profile bug — HIGH**~~ FIXED, commit `8e9b1f2`, follow-up `598aa0a` (2026-08-09).
   Memory: `project_ticket_create_mcp_profile_bug_2026_08_04.md`
   `resolveTicketAdapter()` now threads `cwd` through (folder-based profile resolution, same mechanism Recall already used), plus a project/profile mismatch safety net on `create` specifically, with a working `--profile` override (code-reviewer caught the first version had none). **08-09 follow-up**: that guard's own error message told the caller to pass `--profile=X`, but the MCP tool schema never exposed a `profile` property and never forwarded one — the general architecture was fixed but the MCP surface specifically was not. Closed by mirroring `doctor`'s existing profile-forwarding pattern.

1b. ~~**Secret-scanner Trigger 5 — two adjacent hyphenated compounds false-positive as a secret**~~ FIXED, commits `6977750` (CLI) + `c565b75` (API), hardening follow-up `11aa146` (CLI) + `f7e0fd1` (API), all 2026-08-09.
   Memory: `project_secret_scanner_false_positives_2026_08_06.md`
   Deferred since 2026-08-06 pending a dedicated design pass (3 real-world data points by 2026-08-07). Root cause: two adjacent hyphenated tokens ("REDIS-vs-PG dual-store") were never treated as run-stopping "label words," so they concatenated during the entropy join and crossed threshold with no digits present. Fixed with a narrow `isHyphenatedWordCompound` check (letters-only, hyphen-delimited, segments ≤15 chars) added to `isLabelWord` — provably isolated from the hard-reject pass (`stopAtLabelWords:false` never calls `isLabelWord`). **Hardening follow-up**: a second, deeper adversarial security pass (177,240 probes) found the check had no case-switch guard — a base64-shaped mixed-case fragment (e.g. `zqXvbNmKl-PoIuYtR`) would read as a "compound word" purely for containing a hyphen, same bypass category as the prior CRITICAL code-filename exemption. Closed by adding `hasInternalCaseSwitch` guard (matches the pattern used everywhere else in the file). Same review surfaced a pre-existing, unrelated HIGH — see item 1c below.

1c. ~~**PHP secret scanner: Unicode whitespace does not split tokens (ASCII-only regex) — HIGH, pre-existing.**~~ FIXED, `ticketlens-api@724f41c`, 2026-08-09.
   `RecallSecretScanner.php`'s two `\s+` regexes (tokenize + despace) got `/u`, extracted to a `WHITESPACE_RE` const. Adversarial security review of the fix itself (not scoped in advance, run because this touches the same file as 1b) found two follow-on issues, both closed in the same commit:
   - `/u` alone doesn't cover `U+FEFF` (BOM) — PCRE's non-UCP `\s` table excludes it, JS's `\s` spec includes it. Live-verified bypass: `AKIAAAAAAAAA<U+FEFF>AAAAAAAA` passed clean pre-fix. Closed by adding `\x{FEFF}` explicitly to `WHITESPACE_RE`.
   - `/u` makes `preg_split`/`preg_replace` return `false` on invalid UTF-8, which crashed `array_filter()` with an uncaught `TypeError` (500) — reachable because the push route accepts form-encoded bodies too, not just JSON, and Laravel's `string` rule doesn't validate encoding. Closed with a new `App\Rules\ValidUtf8` rule on every free-text field in `PushRequest`, so this now 422s at the boundary.
   4 new `RecallSecretScannerTest` cases (00A0/3000/2028/FEFF), 3 new `ValidUtf8Test` cases, 1 new `PushControllerTest` boundary case. 64/64 scoped suite green. No CLI/JS change — JS's `\s` was already correct on both points.

1d. ~~**PHP + JS secret scanner: PEM private-key pattern is fully bypassable with any embedded whitespace — HIGH, pre-existing, both languages.**~~ FIXED + PUBLISHED + LIVE-VERIFIED, `ticket-lens@01db958`, `ticketlens-api@0b424ad`, `ticketlens@0.38.25` (beta).
   `HARD_REJECT_PATTERNS`' PEM entry was the only pattern with **literal embedded spaces** (`-----BEGIN (RSA |EC |...)?PRIVATE KEY-----`), unlike the other four (contiguous character classes). Both scanners' whitespace-tolerant fallbacks defeated their own PEM pattern: `hardRejectRuns`/rejoin concatenates tokens with no separator (destroyed the required spaces), and despacing strips all whitespace (same problem). Fix: widened the PEM regex's internal spacing to a whitespace-tolerant class (`\s*` in JS — natively Unicode-aware; a new shared `WHITESPACE_CLASS` const `[\s\x{FEFF}]` reused by `WHITESPACE_RE` in PHP, with `/u`) instead of adding new derived-string checks — the same three existing check surfaces (`combined`/`hardRejectRuns`/`despacedCombined`) now catch every variant with one change. code-reviewer + security-reviewer both APPROVE, 0 CRITICAL/HIGH (security-reviewer adversarially tested word-insertion, case-sensitivity, all 4 algorithm variants, U+200B — confirmed those are pre-existing, unrelated, already-tracked gaps, not regressions or new holes). 58/58 JS, 48/48 PHP. Live-verified against the real installed CLI and the real running API (`HTTP 422` on the exact reported repro, `HTTP 200` on a clean note) — test fixtures cleaned up after. Memory: `project_secret_scanner_false_positives_2026_08_06.md`.

1e. ~~**PHP + JS secret scanner: U+200B (zero-width space) not treated as whitespace — LOW, pre-existing, file-wide, both languages.**~~ FIXED + LIVE-VERIFIED, `ticket-lens@ec3d0c3`, `ticketlens-api@f2a20c4`.
   Found during 1d's security review (not caused by it). U+200B is Unicode category Cf (format), not Zs (space separator) — excluded from JS's native `\s` and from PHP's `WHITESPACE_CLASS`/`WHITESPACE_RE`. Affects all 5 `HARD_REJECT_PATTERNS` entries equally (not PEM-specific), same underlying whitespace definition used everywhere in the file. Fix: JS gained the shared `WHITESPACE_CLASS`/`WHITESPACE_SPLIT_RE`/`WHITESPACE_STRIP_RE` consts it never previously needed (mirroring PHP's existing pattern); PHP was a one-line widen of the existing `WHITESPACE_CLASS` to `[\s\x{FEFF}\x{200B}]`. Reviewed inline (both dispatched code-reviewer/security-reviewer background agents failed three times in a row on the account's Claude session limit / an unrelated dispatch issue — reviewed directly instead, including 14 JS + 16 PHP adversarial bypass attempts executed live, byte-level PCRE verification of `\x{200B}`, and a check that ZWSP-splitting a label word can't weaken adjacent secret detection). One real test-rigor gap self-caught and fixed (PHP's PEM test wasn't asserting the specific reason text, same class as the JWT test caught during RED). 3033/3033 CLI, 54/54 PHP scanner file. Live-verified against the real CLI (`note add`) and the real running API (`POST /v1/recall/push`, `HTTP 422` on the exact repro, `HTTP 200` on a clean note) — test fixtures cleaned up after. Memory: `project_secret_scanner_false_positives_2026_08_06.md`.

2. ~~**Stop hook re-nags across a session_id rollover — HIGH**~~ FIXED, commit `e06c6e5`.
   Memory: `project_recall_nudge_session_boundary_gap_2026_08_05.md`
   Cross-session capture marker keyed by cwd (not session_id), 2-hour freshness window, stored in a user-private mode-0700 tmp subdirectory.

2b. ~~**PostToolUse recall-nudge hook is dead code for MCP-first usage**~~ REMOVED, commit `e8cd5cc`.
   Memory: `project_recall_nudge_posttool_hook_dead_2026_08_05.md`
   Deleted outright per user decision. `hooks-setup.mjs` now cleans up the stale entry from existing installs, only touching its own inner hook when colocated with a user's own hook (code-reviewer caught a MEDIUM here too).

## Features — scoped, ready to plan

3. ~~**`ticketlens doctor` diagnostic command** (ROADMAP 49c)~~ SHIPPED.
   Spec: `docs/superpowers/specs/2026-08-04-ticketlens-doctor-design.md`
   5 v1 checks, opt-in `--fix` for 3. MCP handshake + MCP registration checks were deliberately deferred out of v1 — both shipped 2026-08-08 as ROADMAP 49k (see `project_doctor_mcp_registration_handshake_2026_08_08.md`).

## Features — scoped, ready to plan

4. **Full MCP tool coverage** (ROADMAP 49b) — SCOPED 2026-08-11, `fetch`+`triage`+`compliance`+`standup`+`pr`+`review`+`stats`+`history`+`collisions` (9 of 11) SHIPPED 2026-08-12, all local/not pushed. `stats`/`history`/`collisions` pass also fixed a live CRITICAL: `history`'s ticket arg matching `"--help"` leaked to real stdout (JSON-RPC channel), plus the identical pre-existing bug in already-published `fetch`/`compliance`/`pr`. 2 tools remain: `ledger`, `recall_update`/`recall_delete`. Memory: `project_mcp_tool_coverage_scoping_49b_2026_08_11.md`, `project_fetch_mcp_tool_shipped_2026_08_12.md`, `project_triage_mcp_tool_shipped_2026_08_12.md`, `project_compliance_mcp_tool_shipped_2026_08_12.md`.
   Current MCP surface is 19 tools, against 21 remaining unwrapped CLI surfaces. Port order `fetch`/`triage` first → `ledger` last: ~~`fetch`~~ ~~`triage`~~ ~~`compliance`~~ ~~`standup`~~ ~~`pr`~~ ~~`review`~~ ~~`stats`~~ ~~`history`~~ ~~`collisions`~~ done, `ledger`, plus `recall_update`/`recall_delete` (fixes a hidden gap — `recall_add` lets an agent create a note but nothing lets it fix or remove one). Gated flags on `triage`/`stats` exposed immediately, not deferred — `triage` ended up with 4 more gated fields than originally scoped (`project`/`label`/`priority` Team, `sort` Free — all missed in the 2026-08-11 scoping pass). `sync`, `recall sync`, `recall settings`, and the original admin-command set stay CLI-only. `compliance` reused `fetch`'s underlying `run()` (just another subcommand) and found a second stream leak beyond the direct `process.stderr.write` sites: `runComplianceCheck` was never passed a `stream` at all, so its Pro-gate upgrade prompt always hit real stderr. `ticket-lens@5c5a8df` (local, not pushed/published). `standup`/`pr`/`review` reused the same pattern — code review caught the identical leak class again in `pr` (this time via `assemblePr`'s internal `runComplianceCheckFn` call, one function away from the code being edited), fixed with a wrapping closure. `review`'s Pro-gate confirmed NOT sharing `compliance`/`pr`'s monthly counter — a plain license check; doc wording tightened after review flagged it as ambiguous. `stats`/`history`/`collisions` shipped next: `history` had no CLI-layer function at all (extracted from an inline `bin/ticketlens.mjs` case into new `run-history.mjs`), `collisions` needed the returned `{ok}` for success (its own failures print, don't warn). Code review caught a CRITICAL — `history`'s `ticket` arg matching `"--help"`/`"-h"` leaked straight to real stdout, corrupting the JSON-RPC channel — and, live-reproduced during the same check, the identical pre-existing bug already live in published `fetch`/`compliance`/`pr`. Fixed both with explicit user sign-off to expand scope beyond the original plan. Local only, not pushed/published. 2 tools remain: `ledger`, `recall_update`/`recall_delete`.
5. ~~**Configurable Recall capture strictness**~~ SHIPPED 2026-08-12, `ticket-lens@05ccb95`, published `ticketlens@0.38.29` (beta tag). Resolved: single scalar (`loose|balanced|strict`), per-profile local storage in `profiles.json` (not Console-synced — the Stop hook fires on every session end and must stay network-free), dual-lever enforcement (SKILL.md capture-bar calibration + a narrowed Stop-hook trigger for `loose` only — `strict` deliberately never widens the hook beyond `balanced`, see design spec §5 for why). Full subagent-driven-development execution: 10 tasks + a final whole-branch review (0 Critical, 2 Important, 4 Minor) + one fix wave. 3233→3282 tests. New backlog item #12 filed (multi-profile users can have the two levers resolve different profiles — documented, deliberately deferred).
6. **Recall note attachments** (screenshots/files on `note add`) — memory: `project_recall_attachments_backlog_2026_08_04.md`. Distinct from the shipped ticket_create/ticket_comment attachment feature. Storage, size limits, Pro-gating all undecided.
7. **Multi-agent AI consensus compliance**, `--consensus` (ROADMAP 24b) — route compliance checks through 3-5 BYOK AI agents. Needs its own research pass (providers, disagreement reconciliation, cost, tier gating).

## Small / deferred

8. ~~**cli-token.json tier-spoofing audit + fix**~~ FIXED 2026-08-10. Memory: `project_cli_token_tier_spoof_audit_backlog_2026_08_05.md`.
   Audit confirmed the deferred question: `history`, `schedule --local`, and `review`'s requirements-coverage section are all Pro-gated local-only codepaths (zero network) that a hand-edited `tier` field unlocked for free. While designing the fix, also found `license.json`'s `readLicense()` trusted any file with no `sig` field at all (forge-from-scratch, not just tamper), and — caught by security-reviewer, not the original audit — the "legacy key-based HMAC" fallback signed with a key derived from `payload.key`, a field inside the same untrusted payload, meaning ANY license.json could be self-forged with zero prior state (fresh install) using only the public salt constant. All three closed: extracted machine-secret HMAC helpers into new `machine-secret.mjs` (shared by `license.mjs` and `cli-auth.mjs`, avoids a circular import), `cli-token.json`'s `tier` now signed with a distinct salt (`tl-tok-v1` vs license's `tl-lic-v1`, cross-file replay tested explicitly), `license.json` unsigned/legacy-scheme trust removed entirely — verification now always requires a real machine-secret signature. 3053/3053 tests passing (was 3033), code-reviewer + security-reviewer both ran to completion (security-reviewer's first pass is what caught the fresh-install forgery — a fast APPROVE was not the ceiling). Two lower-urgency findings explicitly deferred by the user, see item 8b below.

8b. ~~**Team-seat tier expiry + sync-can't-downgrade**~~ FIXED, `ticket-lens@c171191`, `ticketlens-api@3ab9d79`, 2026-08-11.
   Scope grew mid-session: while verifying the MEDIUM item's "is this reachable" question, found the real bug was server-side, not CLI-side — `MembersController::destroy()` (team member removal) detached the group pivot and cleared one permission bit, but never reset the removed member's `tier` column or the rest of their `permissions` bitmask. Since `PermissionService::effective()` ORs a user's own stored permissions with their group's, a removed member kept full Team-tier access indefinitely regardless of group membership — live and reachable via `/analytics` and `/compliance`/`/v1/compliance`, zero exploit needed. Backend fix reuses the existing, already-tested license-revocation downgrade logic (`LicenseIssuanceService`'s private `syncUserTierAfterRevoke` promoted to public `syncUserTierToOwnLicenses`, exclusion param now optional) rather than writing new logic — correctly preserves a member's own separate active license if they hold one. Confirmed the original MEDIUM's premise was wrong in the CLI's favor: `users.tier` is `NOT NULL DEFAULT 'free'`, so `/v1/profiles` never actually omits `tier` — fixed defensively anyway (never trust external data at the boundary). HIGH's grace period set to 7 days (shorter than `license.json`'s 30 — team removal is a security action, not a billing lapse), signed together with `syncedAt` so it can't be tampered forward independently; also wired tier refresh into `checkTeamMembershipUpdate` (already fires on every `fetch`) so the grace period is a backstop, not the only defense. Both product decisions (grace-period length, wiring into checkTeamMembershipUpdate) made by the user before implementation.
   code-reviewer + security-reviewer both APPROVE, 0 CRITICAL/HIGH. Two independent reviewers converged on the same MEDIUM (missing row lock in `destroy()` vs. `revoke()`'s existing `lockForUpdate()` pattern) — fixed. Security-reviewer found one MEDIUM left deliberately unfixed: the CLI grace period trusts local wall-clock time, so a removed user can restore their own stale access by rolling back their OS clock — same pre-existing weakness class as `license.json`'s `GRACE_DAYS`, not new, not blocking. Also one LOW: re-inviting a previously-removed member no longer auto-restores their tier (fails toward less access, functional gap not security bug) — needs its own product decision, not filed as a numbered backlog item yet. Live-verified end-to-end via tinker against the real DI-resolved controller + real `/v1/profiles` HTTP response (rolled back, no residue): `tier: "team"` → `"free"` after removal. CLI: 3053→3063 tests. Backend: 1623→1628 tests, 7 pre-existing unrelated failures (Slack OAuth, Triage push) confirmed present at baseline before any of these changes.
9. ~~**Switch `ticketlens` MCP server from project-scope to user-scope**~~ FIXED 2026-08-11. `claude mcp add -s user ticketlens -- ticketlens mcp` registered once in `~/.claude.json`; removed the redundant project-scoped `.mcp.json` copies in `ticket-lens`, `ticketlens-api`, and `solopreneur` (all untracked/local-only, no git impact). Verified user-scoped and connected via `claude mcp get ticketlens`.

10. ~~**CLI team-seat grace period trusts local wall-clock time — MEDIUM.**~~ FIXED + PUBLISHED, `ticket-lens@d6b5593`, published `ticketlens@0.38.28` (beta tag).
    `readCliTokenTier` (`cli-auth.mjs`) computed days-since-sync purely from `Date.now()` vs. the signed `syncedAt`, with no trusted/monotonic time source, letting a revoked team member restore stale local entitlement by rolling back their OS clock. User's decision: monotonic high-water mark, not the mtime-comparison alternative. Fix adds a third signed field `freshMark` (HMAC-covered alongside `tier`/`syncedAt`, same salt/machine-secret scheme) — ratcheted forward on every honest read, read rejected outright if `Date.now() < freshMark`. `saveCliToken()` resets `freshMark = syncedAt` on every real login/sync (a trusted server round-trip); the ratchet write is local-read-only and never touches `syncedAt`, so the 7-day grace window itself can't be extended just by running gated commands offline. A file missing `freshMark` (pre-fix) self-heals via rejection, same pattern as the existing missing-`syncedAt` case. Deliberately accepted residual gap, documented in a code comment: an attacker who fakes the clock from the very first read after a real sync is not caught — inherent to any offline, network-free grace period, same as `license.json`'s pre-existing `GRACE_DAYS` weakness. 4 new tests in `cli-auth.test.mjs` (ratchet-forward, rollback-rejection, missing-freshMark self-heal, saveCliToken always writes freshMark); full CLI suite 3067/3067. Live-verified through the real `license.mjs::isLicensed()` caller (not just unit tests): happy path `true`, simulated rollback `false`. Memory: `project_cli_token_tier_spoof_audit_backlog_2026_08_05.md`.

11. ~~**Re-inviting a removed team member doesn't restore their tier — LOW, functional gap.**~~ FIXED, `ticketlens-api@61f09e9`. Not yet deployed — backend change, no npm publish step; deploy manually when ready.
    `MembersService::invite()` only set `tier`/`permissions` on brand-new user creation; an existing user found by email was only re-attached to the group, tier/permissions left untouched — stuck at `free` after 8b's removal-downgrade fix. User's decision: restore `max(manager's tier preset, member's own active license)` by priority, reusing the ranking `syncUserTierToOwnLicenses` already trusts for the downgrade direction. New `LicenseIssuanceService::restoreTierOnReinvite()`, with `applyTier()` extracted as a shared private helper (`TIER_PRIORITY` const, `allowBootstrap` flag). **Real bug caught mid-implementation, before shipping**: the first draft reused `applyTier`'s bootstrap-into-group-ownership branch unconditionally, which would have made any plain re-invited member into a group owner with manager permission bits whenever the *manager's* group happened to be team-tier — that branch was only ever safe when the tier came from the member's *own* license (true for the removal path, not true here). Caught by manually tracing through an existing, unrelated lock test (`MembersServiceTest::test_invite_is_idempotent_for_existing_user`) that wouldn't have failed on its own. Fixed with an explicit `allowBootstrap` parameter, true only when the member's own license outranks the group preset. 6 new tests across `LicenseIssuanceServiceTest`/`MembersServiceTest`/`MembersControllerTest`, including a regression lock for the caught bug. Live-verified via `sail artisan tinker` against the real DI-resolved service chain, both directions (own-license-wins → bootstrapped; group-preset-wins → plain member, no bootstrap), rolled back, no residue. Memory: `project_cli_token_tier_spoof_audit_backlog_2026_08_05.md`, `note "Shared tier-application helper conflated two different semantic sources of the same tier value"` (Recall).

    **Review note**: both fixes shipped without an agent code-reviewer/security-reviewer sign-off — 5 consecutive review-agent dispatches (3 code-reviewer: background/foreground/background retry, 2 security-reviewer: background/background retry) went idle with no findings ever delivered this session, despite direct pings and explicit stop+respawn between attempts. User explicitly chose to ship without a review pass after exhausting retries rather than keep spawning. Manual verification (full test suites, live tinker through the real DB) stands in for the missing agent review.

12. ~~**Recall-nudge Stop hook profile resolution doesn't thread the matched ticket key — LOW, functional gap.**~~ FIXED + PUSHED + PUBLISHED 2026-08-11, `ticket-lens@80013df` (+ version bump `198e129`), published `ticketlens@0.38.30` (beta tag).
    Found 2026-08-11 during the whole-branch review of "Configurable Recall capture strictness" (ROADMAP 49d, `recall-capture-strictness` worktree). `resolveProfile(null, { cwd })` in `recall-nudge-stop.mjs` (Lever 2, the Stop hook) never received a ticket key, so it fell through to cwd/`projectPaths` then the default profile — unlike `resolveConnection(ticketKey, ...)` (Lever 1, the brief injection in `profile-resolver.mjs`), which resolves by ticket-key prefix when one is available. Fix: `scanTranscript()` (`recall-nudge-lib.mjs`) now returns a `ticketKey` field (first matched ticket key's literal text, alongside the pre-existing `sawTicketKey` boolean), threaded into `resolveProfile(ticketKey, { cwd })` in the hook — both levers now resolve the same profile. code-reviewer: 0 CRITICAL, 0 HIGH, 1 MEDIUM (stale comment understating the new consequence of the pre-existing broad-match design — fixed by clarifying the comment, not narrowing the match; the ceiling stays "wrong local settings value governs one Stop-hook decision," no credentials/data involved). Two new real-subprocess tests in `recall-nudge-stop.test.mjs` prove the fix in both directions (stricter non-default profile applying, looser non-default profile applying) — verified they fail against pre-fix source via `git stash`. CLI 3116→3121 tests, 0 regressions. Live-verified twice: once pre-publish via direct subprocess invocation, once post-publish against the real globally-installed `ticketlens@0.38.30` package (grepped installed hook for the fix, ran the same BETA-42 scenario against it). See `docs/superpowers/specs/2026-08-11-recall-capture-strictness-design.md` §6 (updated).

13. **Secret scanner: filename immediately followed by an identifier joins into a false-positive, JS + PHP (likely both, unverified on PHP) — HIGH-ish (blocks ordinary Recall note-taking), pre-existing.**
    Found 2026-08-11 dogfooding `note add` while writing a Recall note about the MCP scoping pass (item 4). Root cause distinct from every 1a–1e mechanism — not a regression. `joinedChunkRuns` (secret-scanner.mjs:263) glues up to `MAX_JOINED_CHUNKS=4` adjacent whitespace-split tokens with no separator, to catch a secret deliberately fragmented across spaces. `looksLikeCodeFilename`'s exemption requires the WHOLE token to match `^[A-Za-z]+\.(ext)$` end-to-end — once a joined candidate has anything trailing after the extension (any word immediately following a filename mention), the exemption can never match, filename shape or not. Concrete repro: the sentence `note-command.mjs's runNoteAdd` (an entirely ordinary way to reference a function by its file) joins to the candidate `note-command.mjs'srunNoteAdd` — 28 chars, Shannon entropy 3.91, past the 3.75 threshold, rejected as a secret. Verified via direct `scanForSecrets()` reproduction + token-window bisection, not guessed. Distinct from the already-known-but-unfiled sibling gap ("a long hyphenated tag can trip the check standalone" — compound-word exemption stops it from joining a run, never exempts it alone) — that one is about tags; this one is about body prose, and about the exemption's shape requirement rather than run-joining eligibility. Practical impact: "filename.ext's/then/before nextIdentifier" is normal engineering-note phrasing — exactly what Recall exists to capture — so this will keep tripping real, non-secret notes. User decision 2026-08-11: file now, fix in a later dedicated pass (not this session).

---
**Status 2026-08-12**: #1, #1b, #1c, #1d, #1e, #2, #2b, #3, #5, #8, #8b, #9, #10, #11, #12 shipped — all of backlog item 1 (1a–1e) closed, item 8 (8+8b) fully closed, item 10 and 11 (found during 8b) fully closed, #9 closed, #5 (ROADMAP 49d) closed, #12 (found during #5's review) closed. #4 (ROADMAP 49b) scoped 2026-08-11, 9 of 11 tools ported (`fetch`+`triage`+`compliance`+`standup`+`pr`+`review`+`stats`+`history`+`collisions`). Remaining: implement #4's other 2 tools (`ledger`, `recall_update`/`recall_delete`), scope #6/#7, fix #13.
